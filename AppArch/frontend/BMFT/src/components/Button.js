import React from 'react'

const Button = ({type, variant, className, id, oneClick, children}) => {
  return (
    <div className="App">
        <Button>Subscribe</Button>
         </div>
  )
}

export default Button